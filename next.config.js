/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    // basePath: '/metro',
    // skipTrailingSlashRedirect: false,
    //trailingSlash: false
}

module.exports = nextConfig
