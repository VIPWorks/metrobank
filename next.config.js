/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/metro',
    trailingSlash: false,
    skipTrailingSlashRedirect: true,
    output: 'export'
}

module.exports = nextConfig